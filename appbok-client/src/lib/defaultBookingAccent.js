/**
 * Standard accent (Colorisma) — varm, tydlig brun för knappar / CTA / preview.
 * Server: `server/lib/defaultBookingAccent.js`.
 */
export const DEFAULT_BOOKING_ACCENT_HEX = '#6f4e37';

/** Tidigare standard — upplevdes som gråbeige; byts till DEFAULT vid merge/resolve. */
export const LEGACY_BOOKING_ACCENT_HEX = '#8c7462';

/** Äldre DB-seed / Lux-beige; byts till varm brun i linje med nya standarden. */
export const LEGACY_BEIGE_ACCENT_HEX = '#a89483';
